# Copy to .env.local to override at dev time.

# Default points at the zero-dep scaffold (apps/server, port 4000).
# Switch to http://localhost:4001 to consume the NestJS backend.
VITE_API_BASE_URL=http://localhost:4000
